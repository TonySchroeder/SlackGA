import { FilterThreadsPipe } from './filter-answers.pipe';

describe('FilterAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterThreadsPipe();
    expect(pipe).toBeTruthy();
  });
});
