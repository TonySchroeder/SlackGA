import { FilterFirstmessagePipe } from './filter-firstmessage.pipe';

describe('FilterFirstmessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFirstmessagePipe();
    expect(pipe).toBeTruthy();
  });
});
