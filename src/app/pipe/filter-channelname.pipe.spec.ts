import { FilterChannelnamePipe } from './filter-channelname.pipe';

describe('FilterChannelnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChannelnamePipe();
    expect(pipe).toBeTruthy();
  });
});
