import { FilterUserMailPipe } from './filter-user-mail.pipe';

describe('FilterUserMailPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserMailPipe();
    expect(pipe).toBeTruthy();
  });
});
