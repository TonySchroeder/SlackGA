import { FilterFirstmessageUserPipe } from './filter-firstmessage-user.pipe';

describe('FilterFirstmessageUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFirstmessageUserPipe();
    expect(pipe).toBeTruthy();
  });
});
