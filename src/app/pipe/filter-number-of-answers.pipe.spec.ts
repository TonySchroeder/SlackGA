import { FilterNumberOfAnswersPipe } from './filter-number-of-answers.pipe';

describe('FilterNumberOfAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNumberOfAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
