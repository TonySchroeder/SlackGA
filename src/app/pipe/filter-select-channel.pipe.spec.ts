import { FilterSelectChannelPipe } from './filter-select-channel.pipe';

describe('FilterSelectChannelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectChannelPipe();
    expect(pipe).toBeTruthy();
  });
});
