import { FilterSelectThreadPipe } from './filter-select-thread.pipe';

describe('FilterSelectThreadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectThreadPipe();
    expect(pipe).toBeTruthy();
  });
});
